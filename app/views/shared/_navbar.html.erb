<!-- Navigation -->

<nav class="navbar navbar-default navbar-fixed-top topnav spexial" role="navigation">
  <div class="container topnav">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%#= image_tag "GoatFace.png", height: "50px" %>
      <a class="brand" style="margin: 0; float: none;" href="/"><%= image_tag "wholelogo.png", height: "60px" %></a>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <li>
          <a href="http://www.npr.org/sections/goatsandsoda/2014/07/08/329578819/whats-in-our-name-why-goats-why-soda">About</a>
        </li>
        <li>
        <% if current_user %>
          <%= link_to "Profile", user_path %>
        <% end %>
        </li>
        <li>
        <% if current_user_guest || current_user.donor? %>
          <%= link_to "Recipients", users_path %>
        <% elsif current_user && current_user.recipient? %>
          <%= link_to "Needs Catalog", needs_path %>
        <% end %>
        </li>
        <li>
        <% if current_user_guest || current_user.donor? %>
          <%= link_to "Basket: #{@cart.count_donations}", cart_path%>
        <% elsif current_user.recipient? %>
          <%= link_to "Basket: #{@cart.count_needs}", cart_path, class: 'basket-count' %>
        <% else %>
          <%= link_to "Basket", cart_path %>
        <% end %>
        </li>
        <li>
          <% unless current_user %>
            <%= link_to "Login or Create Account", login_path %>
          <% else %>
            <%= link_to "Logout #{current_user.first_name.capitalize}", logout_path %>
          <% end %>
        </li>
      </ul>
    </div>
    <!-- /.navbar-collapse -->
  </div>

  <% if current_admin? %>
    <div class="btn-group btn-group-justified" role="group" aria-label="...">
      <div class="btn-group" role="group">
        <%= link_to "Admin Home", admin_profile_path, class: "btn btn-default" %>
      </div>
      <div class="btn-group" role="group">
        <%= link_to "All Need Types", admin_need_types_path, class: "btn btn-default"  %>
      </div>
      <div class="btn-group" role="group">
        <%= link_to "Add Need to Catalog", new_admin_need_type_path, class: "btn btn-default" %>
      </div>
      <div class="btn-group" role="group">
        <%= link_to "All Users", admin_users_path, class: "btn btn-default"  %>
      </div>
      <div class="btn-group" role="group">
        <%= link_to "Manage Recipients", admin_recipients_path, class: "btn btn-default"  %>
      </div>
      <div class="btn-group" role="group">
        <%= link_to "Add New User", new_admin_user_path, class: "btn btn-default"  %>
      </div>
    </div>
  <% else %>
  <% end %>
</nav>
